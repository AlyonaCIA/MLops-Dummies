apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
data:
  nginx.conf: |
    # Sample Nginx configuration
    server {
      listen 80;
      location / {
        root /usr/share/nginx/html;
      }
    }
